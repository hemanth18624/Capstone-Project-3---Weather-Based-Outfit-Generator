<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather Based Outfit Generator</title>
    <link rel = "stylesheet" type="text/css" href = "styles/main.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
</head>
<body>
    <div class="heading">
        <h1>Weather Based Outfit Generator</h1>
    </div>
    <div class="box">
        <form method = "post" action = "/results">
            <input type = "text" name = "place" value="<%= city || '' %>" placeholder="Enter city" required>
            <select id = "gender" name = "gender" value="<%= gender || '' %>" required>
                <option value = "">Choose gender</option>
                <option value = "male">Male</option>
                <option value = "female">Female</option>
            </select>
            <button type = "submit" value = "search">Search</button>
        </form>
        <div class="output">
            <h3>
                <ul>
                    <% for( let index = 0; index < content.length; index++ ) { %>
                            <li> <%= content[index] %></li>
                        <% } %>
                </ul>
            </h3>
            <h2 align = "center"> <%= extraContent %></h2>
        </div> 
    </div>
</body>
</html>